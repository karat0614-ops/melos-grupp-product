# Токены и ключи — никогда в репозиторий.
# Токен телеграм-бота от BotFather живёт здесь (шаг 6 плана).
.env
.env.*

# Заявки клиентов: имена, телефоны, Telegram.
# Репозиторий публичный (нужен для бесплатного хостинга страницы) —
# персональные данные заказчиков в него попадать не должны.
leads.json
*.broken-*

node_modules/
